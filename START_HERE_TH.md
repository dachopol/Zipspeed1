# Zipspeed — ปรับเรือนไมล์และปุ่มใหม่

เปิด index.html เพื่อลองหน้าตา หรือรัน `python3 -m http.server 8080` ในโฟลเดอร์นี้แล้วเปิด http://localhost:8080

- เรือนไมล์มีขอบโลหะ แสงเงาและเข็มจริงใน SVG/Compose Canvas เป็นภาพมีมิติที่วาดด้วยโค้ด ไม่ใช่โมเดล Blender หรือไฟล์ .blend
- ปุ่ม GO/STOP จุดเดียว: เริ่ม หยุด และลองใหม่ ไม่มีชั้นโฆษณาทับเรือนไมล์
- เข็มตอบสนองต่อข้อมูลความเร็ว ไม่สร้างค่าจำลองในแอป ช่วง latency แสดงสถานะรอ
- ตั้งค่าพื้นฐานอยู่ด้านหน้า ตั้งค่าขั้นสูง/ความช่วยเหลือพับไว้ ฟีเจอร์ native และเมนูเดิมยังอยู่
- มีโลโก้และไอคอนที่ให้มา พร้อมพื้นหลังนิ่ง ไม่มีดาวน์โหลดฉากหลังระหว่างทดสอบ

ตรวจผ่านในเบราว์เซอร์อัตโนมัติด้วยข้อมูลควบคุม: ปุ่มเดียว เข็มขยับ ทดสอบจบ หยุด/เริ่มใหม่ ล้างค่าเมื่อผิดพลาด ภาษา ธีม ขนาดจอ 390/320px และไม่พบ JavaScript page error ผลนี้ไม่ใช่การยืนยันความแม่นยำบนอินเทอร์เน็ตจริง

Android: ยังไม่ได้ compile หรือทดสอบบนมือถือ ไม่มี APK/AAB ในชุดนี้ โครงงานยังเป็น Kotlin/Compose ไม่ได้ย้าย Flutter เปิด root ใน Android Studio แล้วติดตั้ง SDK/Gradle ให้ตรงโครงการ ZIP ต้นทางไม่มี wrapper scripts/JAR ครบ ต้องสร้าง wrapper ด้วย Gradle ที่ตรง gradle-wrapper.properties ก่อนใช้ `./gradlew :app:assembleDebug`

ภาพ preview-dark.png / preview-light.png เป็นภาพจากหน้าเว็บจริง ใช้ภาษาอังกฤษเพราะเครื่องทดสอบไม่มีฟอนต์ไทย ระบบแอปยังรองรับภาษาไทย

รายการแก้ไข: CHANGED_FILES.txt / redesign.patch
ทดสอบซ้ำบนเครื่องพัฒนา: ติดตั้ง playwright และ browser แล้วรัน `node tests/ui-regression.cjs`
